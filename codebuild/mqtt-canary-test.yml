version: 0.2
phases:
  build:
    commands:
      - python3 -c "from urllib.request import urlretrieve; urlretrieve('https://d19elf31gohf1l.cloudfront.net/releases/v0.9.15/builder.pyz?run=2297453248-1329', 'builder.pyz')"
      - python3 builder.pyz build -p aws-c-http
  post_build:
    commands:
      - echo Build completed on `date`