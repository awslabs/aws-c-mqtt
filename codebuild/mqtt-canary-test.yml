version: 0.2
env:
  variables:
    CANARY_DURATION: 60
    CANARY_THREADS: 3
    CANARY_TPS: 50
    CANARY_CLIENT_COUNT: 10
    CANARY_LOG_FILE: 'canary_log.txt'
    CANARY_LOG_LEVEL: 'TRACE'
    BUILDER_VERSION: v0.9.15
    BUILDER_SOURCE: releases
    BUILDER_HOST: https://d19elf31gohf1l.cloudfront.net
    PACKAGE_NAME: 'aws-c-mqtt-staging'
    CANARY_TEST_EXE_PATH: 'build/install/bin/mqtt5canary'
    CANARY_SERVER_ARN: Mqtt5MosquittoSever
    CANARY_BUILD_S3_DST: mqtt5-canary/s3/build
    
phases:
  install:
    commands:
      # install cmake for codebuild environment.
      - apt-get install build-essential libssl-dev -y -f
      - apt-get update -y
      - apt-get install cmake -y -f
      # Install necessary lib for canary wrapper
      - sudo apt-get install gcc python3-dev -y -f
      - sudo apt-get install pip -y -f
      - python3 -m pip install psutil
      - python3 -m pip install boto3

  build:
    commands:
      - export CANNARY_TEST_EXE=$CODEBUILD_SRC_DIR/$PACKAGE_NAME/$CANARY_TEST_EXE_PATH
      - echo $CANNARY_TEST_EXE
      - export ENDPOINT=$(aws secretsmanager get-secret-value --secret-id "$CANARY_SERVER_ARN" --query "SecretString" | cut -f2 -d":" | sed -e 's/[\\\"\}]//g')
      - export S3_DST=$(aws secretsmanager get-secret-value --secret-id "$CANARY_BUILD_S3_DST" --query "SecretString" | cut -f2,3 -d":" | sed -e 's/[\\\"\}]//g')
      - echo $S3_DST
      - export GIT_HASH=$(git rev-parse HEAD)
      - echo $GIT_HASH
      # Build library and test
      - python3 -c "from urllib.request import urlretrieve; urlretrieve('$BUILDER_HOST/$BUILDER_SOURCE/$BUILDER_VERSION/builder.pyz?run=$CODEBUILD_BUILD_ID', 'builder.pyz')" 
      - python3 builder.pyz build -p aws-c-mqtt --cmake-extra=-DENABLE_SANITIZERS=true --cmake-extra=-DSANITIZERS=",address,undefined"

  post_build:
    commands:
      - echo Build completed on `date`
